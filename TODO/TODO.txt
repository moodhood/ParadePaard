TODO:
Hard (Days):
CAO auto hook

Medium (Hours):

Easy (0-60 min):
Add CAO settings (for manual edit)

Work on my history page

Look at payslip for more inspiration UI wise
						
test contract logic

fix, role in application is set by admin

notification 

cao contract 

make sure that there is a balance for the company too!

Build the payroll flow for a Dutch payroll company.

There should be a permission for being able to see id's apart from contracts, so someone with contract permissions should be able the contract, but if they do not have have id visibility permission (or a better name idk) they cannot see the the id of that corresponding employee

So for the location, there should be a list of locations that the admin should be able to choose from for the project/shift part, this list should be a backended list, it should also be tied to the client, so lets say there is a project x, that has client y, and location z, then next time I make a project, with client y, location z should be automatically be at the top of the list. To make it easy to add a location to the list, if it is not in it yet, then you just need to click on the add button next to the location text box that will prompt you a pop up box with fields to fill in which will add that location to the list.

Make the apply form modular, as in the admin can change the apply form.

Make a super super admin, as in a admin that has an interface where they can see, adjust, and do everything with every company, and also create a new company, and onboard for that company, since if there is no user that has any permissions within that company, there is no way to onboard other people to that company.

user page improve contract part